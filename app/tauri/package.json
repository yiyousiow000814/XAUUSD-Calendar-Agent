{
  "name": "xauusd-calendar-agent-tauri",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "prepare:seed-repo": "node ../../scripts/prepare-seed-repo.mjs",
    "dev": "npm run prepare:seed-repo && tauri dev",
    "build": "npm run prepare:seed-repo && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
